# BGGMNIRA Reproducible Materials

This repository contains the reproducible materials for the empirical application of the Bridge Gaussian Graphical Model NodeIdentifyR Algorithm (BGGMNIRA).

The materials provide a step by step R Markdown example showing how to apply BGGMNIRA to the built-in `DAdata` data set from the `GGMNIRA` R package. The example demonstrates how to conduct BGGMNIRA at three analytical levels: node-level GGMNIRA from the joint-network perspective, node-level GGMNIRA from the construct-specific perspective, and construct-level GGMNIRA from the joint-network perspective.

## Contents

* `BGGMNIRA_tutorial.Rmd`  
  The R Markdown source file containing the full reproducible analysis.

* `BGGMNIRA_tutorial.html`  
  The rendered HTML file containing the code, results, figures, and explanatory text.

* `BGGMNIRA_tutorial_files/`  
  Supporting files generated by R Markdown, such as figures and HTML dependencies.

## Analysis overview

The tutorial includes the following steps:

1. load the `GGMNIRA` R package and the built-in `DAdata` data set;
2. estimate a regularized Gaussian graphical model for depression and anxiety symptoms;
3. apply node-level GGMNIRA from the joint-network perspective;
4. apply node-level GGMNIRA from the construct-specific perspective;
5. apply construct-level GGMNIRA from the joint-network perspective;
6. visualize the KL divergence results for all three levels;
7. evaluate the stability of the KL divergence estimates using case-dropping bootstrap;
8. conduct bootstrap difference tests for pairwise differences in KL divergence.

## Installation

The `GGMNIRA` package can be installed from GitHub using:

```r
install.packages("remotes")
remotes::install_github("ywu6888/GGMNIRA")
```


## How to reproduce the analysis

To reproduce the analysis locally:

1. Download or clone this repository.
2. Open `BGGMNIRA_tutorial.Rmd` in RStudio.
3. Install the required packages.
4. Click `Knit` to render the R Markdown document.
5. The analysis will generate an HTML file containing the code, results, figures, and explanatory text.


## Notes on the three BGGMNIRA levels

In `GGM_NIRA()`, the `result_type` argument is used to select the analytical level:

* `result_type = "node_joint"`  
  Computes node-level projected importance in the complete bridge network.

* `result_type = "cross_marginal"`  
  Computes directional cross-construct projected importance. In this mode, `GGM_NIRA()` computes all directional cross-construct paths. A specific path, such as depression to anxiety, can be extracted later using `GGMNIRA_result()` by specifying `source_construct` and `target_construct`.

* `result_type = "construct_joint"`  
  Computes construct-level projected importance in the complete bridge network. The construct-level manipulation is normalized using a unit-norm constraint so that manipulation intensity is comparable across constructs with different numbers of nodes.

## Citation

If you use the `GGMNIRA` package or this reproducible material, please cite the corresponding manuscript and software repository.
